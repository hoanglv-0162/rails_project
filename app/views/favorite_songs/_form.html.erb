<%= form_with(model: favorite_song, local: true) do |form| %>
  <% if favorite_song.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(favorite_song.errors.count, "error") %> prohibited this favorite_song from being saved:</h2>

      <ul>
        <% favorite_song.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :song_id %>
    <%= form.number_field :song_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
