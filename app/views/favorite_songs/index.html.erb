<p id="notice"><%= notice %></p>

<h1>Favorite Songs</h1>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Song</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @favorite_songs.each do |favorite_song| %>
      <tr>
        <td><%= favorite_song.user_id %></td>
        <td><%= favorite_song.song_id %></td>
        <td><%= link_to 'Show', favorite_song %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Favorite Song', songs_path %>
